OPENAI_API_KEY=REDACTED_REMOVED_FROM_REPO
# The real secrets were removed before pushing. Keep them in your local environment only.
# Example usage: set OPENAI_API_KEY in your shell/session before running the server.

GITHUB_APP_ID=REDACTED
GITHUB_APP_INSTALLATION_ID=REDACTED
# GITHUB_APP_PRIVATE_KEY has been removed from the repo. Store it in a secure secret manager.
